<?php

function keytec_preprocess_paragraph(&$vars){

    if (isset($vars['entity_type']) && $vars['entity_type'] == 'paragraph' ) {
        kint($vars);
    }
}

function keytec_preprocess_form_element_label(&$vars){
    $x = $vars;
    $vars['title'] = null;
}
function keytec_preprocess_input__submit(&$vars){
    $x = $vars;
    $vars['attributes'] = null;
}