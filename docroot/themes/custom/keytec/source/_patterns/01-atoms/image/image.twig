<div class="image {{ modifier_class('image', modifier) }} {{ attributes.class }}"{{ attributes|without('class') }}>
    {% if src != null %}
        <img src="{{- src -}}" alt="">
    {% else %}
       {{ img }}
    {% endif %}
</div>